// Define a variável numero como 20
var numero = 20;

// Verifica se uma das condições é verdadeira
if ((numero == 21 && numero - 1 == 20) || numero == 20) {
    // Se verdadeiro, imprime 'O a expressão OR passou'
    console.log('O a expressão OR passou');
} else {
    // Se a condição anterior for falsa, imprime 'Não passou'
    console.log('Não passou');
}
