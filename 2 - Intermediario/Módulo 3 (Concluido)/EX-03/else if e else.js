// Define a variável nome como 'Jefferson'
var nome = 'Jefferson';

// Verifica se o nome é igual a 'Pedro'
if (nome == 'Pedro') {
    // Se verdadeiro, imprime 'O nome não é verídico'
    console.log('O nome não é verídico');
} 

// Se a condição anterior for falsa, verifica se o nome é igual a 'Jefferson'
else if (nome == 'Jefferson') {
    // Se verdadeiro, imprime 'O nome é verídico'
    console.log('O nome é verídico');
}

// Se nenhuma das condições anteriores for verdadeira, executa este bloco de código
else {
    // Imprime 'O nome é outro'
    console.log('O nome é outro');
}
