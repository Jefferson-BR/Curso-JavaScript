// Define a variável nome como 'Matheus'
var nome = 'Matheus';

// Define a variável idade como 20
var idade = 20;

// Verifica se o nome é igual a 'Matheus' e se a idade é igual a 20
if (nome == 'Matheus' && idade == 20) {
    // Se verdadeiro, imprime 'Este é o aluno matriculado'
    console.log('Este é o aluno matriculado');
} else {
    // Se a condição anterior for falsa, imprime 'não é o aluno matriculado'
    console.log('não é o aluno matriculado');
}
