// Declaração de uma variável 'x' usando 'let' com valor 10.
let x = 10;

// Declaração de uma constante 'y' com valor 20.
const y = 20;

// Início de um bloco 'if'
if(true){
    // Declaração de uma variável 'x' dentro do bloco 'if' usando 'let' com valor 25.
    let x = 25;

    // Declaração de uma constante 'y' dentro do bloco 'if' com valor 50.
    const y = 50;

    // Exibindo os valores das variáveis 'x' e 'y' dentro do bloco 'if'.
    console.log(x, y);
}

// Exibindo os valores das variáveis 'x' e 'y' fora do bloco 'if'.
console.log(x, y);
